name: workflow
on: [push]

jobs:
  build:
  strategy:
      matrix:
        python-version: [3.8, 3.9, 3.10]
  runs-on: ubuntu-latest
  steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: we are testing this workflow
        run: echo "we are running this workflow"